import { CenteredLayout } from '@/components/layout';

export default function RecoverEmailLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <CenteredLayout>{children}</CenteredLayout>;
}
